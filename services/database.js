const addNewCommit = (repoId, message, commitType) => {};
const getFileFromMergedRepo = (repoId, fileAddress) => {
  return ["hello", "im hosein"];
};

module.exports = { addNewCommit, getFileFromMergedRepo };
